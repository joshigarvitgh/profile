<div class="about__data">
    <div class="about__info">
        {% for language in site.data.skills-languages %}
        <div class="about__box">
            <i class='bx bx-award about__icon'></i>
            <h3 class="about__title">{{language.name}}</h3>
            <span class="about__subtitle"> {{language.weight}}</span>
        </div>
        {% endfor %}
        {% for framework in site.data.skills-frameworks %}
        <div class="about__box">
            <i class='bx bx-award about__icon'></i>
            <h3 class="about__title">{{framework.name}}</h3>
            <span class="about__subtitle"> {{framework.weight}}</span>
        </div>
        {% endfor %}
        {% for tool in site.data.skills-tools %}
        <div class="about__box">
            <i class='bx bx-award about__icon'></i>
            <h3 class="about__title">{{tool.name}}</h3>
            <span class="about__subtitle"> {{tool.weight}}</span>
        </div>
        {% endfor %}
        
    </div>